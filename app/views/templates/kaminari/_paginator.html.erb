<%
=begin

The container tag
- available local variables
  current_page : a page object for the currently displayed page
  total_pages  : total number of pages
  per_page     : number of items to fetch per page
  remote       : data-remote
  paginator    : the paginator that renders the pagination tags inside

=end
%>

<%= paginator.render do %>
  <nav class="paginate">
    <ul class="paginate__list">
      <li class="paginate__list_item first">
      <%= first_page_tag %>
      </li>
      <% each_page do |page| %>
        <% if page.display_tag? %>
          <li class="paginate__list_item page-number">
            <%= page_tag page %>
          </li>
        <% end %>
      <% end %>
      <li class="paginate__list_item last">
        <%= last_page_tag %>
      </li>
      <li class="paginate__list_item prev">
        <%= prev_page_tag %>
      </li>
      <li class="paginate__list__item page-number-for-sp">
        <%= current_page %> / <%= total_pages %>
      </li>
      <li class="paginate__list_item next">
        <%= next_page_tag %>
      </li>
    </ul>
  </nav>
<% end %>
